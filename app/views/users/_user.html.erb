<div class="u">
  <div class="a">
    <%= link_to image_tag(user.image, :alt => user.name), user.url %>
  </div>
  <div class="nc">
    <%= link_to user.name, user.url %>
  </div>
  <div class="c">
    <% if user.arrived? %>
      Arrived!
    <% else %>
      <%= link_to 'Arrived', users_check_path(user) %>
    <% end %>
  </div>
</div>
