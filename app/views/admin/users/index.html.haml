%h2 participants

.yes
  %h3
    yes
    = "(#{@users[:yes].size})"
  %ul.userlist
    - @users[:yes].each do |user|
      %li.usertile= render :partial => 'user', :locals => { :user => user }

.maybe
  %h3
    maybe
    = "(#{@users[:maybe].size})"
  %ul.userlist
    - @users[:maybe].each do |user|
      %li.usertile= render :partial => 'user', :locals => { :user => user }

.no
  %h3
    no
    = "(#{@users[:no].size})"
  %ul.userlist
    - @users[:no].each do |user|
      %li.usertile= render :partial => 'user', :locals => { :user => user }
